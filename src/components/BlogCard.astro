---
const { post } = Astro.props;
---
<article class="bcard">
  <a href={`/blog/${post.slug}`}>
    <img src={post.image} alt="" loading="lazy" />
    <div class="bcard__body">
      <div class="bcard__meta">{post.author} · {post.date}</div>
      <h4>{post.title}</h4>
    </div>
  </a>
</article>
