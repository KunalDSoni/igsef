# Keep installs resilient on slow or throttled links.
# The Next.js tarball is large; too many parallel sockets starve it and npm
# aborts with EIDLETIMEOUT. These values only affect install behaviour.
maxsockets=3
fetch-timeout=900000
fetch-retries=5
fetch-retry-maxtimeout=120000
