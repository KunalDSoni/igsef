/**
 * Stub for Next.js's `server-only` marker module.
 *
 * `server-only` exists to make a build fail if a server module is imported into
 * a client bundle. It cannot be resolved outside Next's build, so the unit suite
 * aliases it here in order to test server modules directly.
 */
export {};
